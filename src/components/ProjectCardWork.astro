---
import type { Project } from '@/data'

import { fromTo } from '@/data'

import HorizontalCard from './HorizontalCard.astro'

interface Props {
  slug: string
  project: Project
}

const { slug, project } = Astro.props
---

<HorizontalCard
  title={`${project.title}, ${project.customer}`}
  icon={project.icon}
  desc={`${fromTo(project)}, ${project.role}`}
  url={`/projects/${slug}`}
  target="_self"
/>
