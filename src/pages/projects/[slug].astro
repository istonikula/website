---
import Layout from '@/components/Layout/Layout.astro'
import * as data from '@/data'

export function getStaticPaths() {
  return data.projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }))
}

const { project } = Astro.props
---

<Layout title={project.title}>
  <main>
    <article class="prose max-w-[750px] prose-img:mx-auto">
      <h1 class="title my-2 text-4xl font-bold">{project.title}</h1>
      <div>{project.customer}</div>
      <div>
        From {project.from} to {project.to}, <span class="whitespace-nowrap">{project.role}</span>
      </div>
      {
        project.tags?.map((tag) => (
          <a href={`/projects/${tag}`} class="badge badge-outline ml-2 no-underline">
            {tag}
          </a>
        ))
      }
      <div class="divider my-2"></div>
      {project.paragraphs.map((p) => <p>{p}</p>)}
    </article>
  </main>
</Layout>
